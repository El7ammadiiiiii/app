import type { PatternDefinition } from '../common/types';

const doubleTop: PatternDefinition = {
  id: 'double-patterns/double-top',
  title: 'Double Top',
  category: 'double-patterns',
  family: 'chart',
  pythonNames: ['Double Top'],
  pythonCategories: ['double_patterns'],
  proOnly: false,
  defaultEnabled: true,
  tags: ['reversal', 'bearish'],
};

export default doubleTop;
