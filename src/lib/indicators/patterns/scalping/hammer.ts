import type { PatternDefinition } from '../common/types';

const hammer: PatternDefinition = {
  id: 'scalping/hammer',
  title: 'Hammer',
  category: 'scalping',
  family: 'momentum',
  pythonNames: ['Hammer'],
  pythonCategories: ['scalping'],
  proOnly: true,
  defaultEnabled: true,
  tags: ['candle', 'reversal', 'bullish'],
};

export default hammer;
