import type { PatternDefinition } from '../common/types';

const expandingTriangle: PatternDefinition = {
  id: 'triangles/expanding-triangle',
  title: 'Expanding Triangle',
  category: 'triangles',
  family: 'chart',
  pythonNames: ['Expanding Triangle'],
  pythonCategories: ['triangles'],
  proOnly: false,
  defaultEnabled: true,
  tags: ['neutral', 'triangle'],
};

export default expandingTriangle;
