import type { PatternDefinition } from '../common/types';

const descendingTriangle: PatternDefinition = {
  id: 'triangles/descending-triangle',
  title: 'Descending Triangle',
  category: 'triangles',
  family: 'chart',
  pythonNames: ['Descending Triangle'],
  pythonCategories: ['triangles'],
  proOnly: false,
  defaultEnabled: true,
  tags: ['continuation', 'triangle'],
};

export default descendingTriangle;
