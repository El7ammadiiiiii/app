import type { PatternDefinition } from '../common/types';

const supportLevel: PatternDefinition = {
  id: 'levels/support-level',
  title: 'Support Level',
  category: 'levels',
  family: 'level',
  pythonNames: ['Support Level'],
  pythonCategories: ['levels'],
  proOnly: false,
  defaultEnabled: true,
  tags: ['support', 'level'],
};

export default supportLevel;
