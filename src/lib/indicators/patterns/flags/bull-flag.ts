import type { PatternDefinition } from '../common/types';

const bullFlag: PatternDefinition = {
  id: 'flags/bull-flag',
  title: 'Bull Flag',
  category: 'flags',
  family: 'chart',
  pythonNames: ['Bull Flag'],
  pythonCategories: ['flags'],
  proOnly: false,
  defaultEnabled: true,
  tags: ['continuation', 'bullish'],
};

export default bullFlag;
