import type { PatternDefinition } from '../common/types';

const bearFlag: PatternDefinition = {
  id: 'flags/bear-flag',
  title: 'Bear Flag',
  category: 'flags',
  family: 'chart',
  pythonNames: ['Bear Flag'],
  pythonCategories: ['flags'],
  proOnly: false,
  defaultEnabled: true,
  tags: ['continuation', 'bearish'],
};

export default bearFlag;
