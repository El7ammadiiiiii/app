"use client";

import React from 'react';
import { CandleData } from './types';

interface PatternCardsProps
{
  data: CandleData[];
  symbol: string;
}

export const PatternCards: React.FC<PatternCardsProps> = ( { data, symbol } ) =>
{
  return null;
};
